import { ToggleStatusButtonDirective } from './toggle-status-button.directive';

describe('ActiveInactiveButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleStatusButtonDirective();
    expect(directive).toBeTruthy();
  });
});
