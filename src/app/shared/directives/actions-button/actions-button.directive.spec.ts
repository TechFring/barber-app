import { ActionsButtonDirective } from './actions-button.directive';

describe('ActionsButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new ActionsButtonDirective();
    expect(directive).toBeTruthy();
  });
});
