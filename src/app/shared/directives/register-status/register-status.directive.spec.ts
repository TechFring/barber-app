import { RegisterStatusDirective } from './register-status.directive';

describe('RegisterStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new RegisterStatusDirective();
    expect(directive).toBeTruthy();
  });
});
