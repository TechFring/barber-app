import { ActiveInactiveButtonDirective } from './active-inactive-button.directive';

describe('ActiveInactiveButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new ActiveInactiveButtonDirective();
    expect(directive).toBeTruthy();
  });
});
